# ── Core Neural Stack ────────────────────────────────────────────────────────
# Base machine learning environment (optimized for CPU/MPS on Mac ARM64)
torch
torchaudio
torchvision

# ── Neural Engine Dependencies (Critical Fixes) ──────────────────────────────
# Fairseq must be installed from source to bypass PyPI wheel metadata errors on ARM64.
# v0.12.2 is the most stable release for RVC-based models.
fairseq @ git+https://github.com/facebookresearch/fairseq.git@v0.12.2

# Custom Transformers for model compatibility
transformers==5.3.0

# ── Audio Processing & Feature Extraction ────────────────────────────────────
librosa
sounddevice
soundfile
scipy
numpy
numba
llvmlite
resampy
soxr
audioread
speechbrain
torchcrepe
einops
local-attention

# ── Utilities, Math & Formatting ──────────────────────────────────────────────
huggingface_hub
faiss-cpu
PyYAML
tqdm
requests
psutil
rich
coloredlogs
filelock
dataclasses-json
packaging
fastapi
uvicorn
python-socketio
websockets
tokenizers
safetensors
bitarray
cython
hydra-core==1.0.7
omegaconf==2.0.6
sacrebleu
